%ul.nav.navbar-nav.navbar-right
  -if employee_signed_in?
    %li=link_to 'My Profile', edit_employee_registration_path
    %li 
      %a#dropdownMenu3.dropdown-toggle.pointer{"aria-expanded" => "true", "data-toggle" => "dropdown", type: "button"}
        %span.glyphicon.glyphicon-user
        = current_employee.email
        %span.caret
      
      %ul.dropdown-menu.navbar-inverse.navbar-nav{'aria-labelledby' => 'dropdownMenu3', role: 'menu' }
        %li{role: 'presentation'}
          = link_to destroy_employee_session_path, method: :delete do
            %span.glyphicon.glyphicon-off
            Sign Out
  -else
    %li= link_to 'Sign In', new_employee_session_path